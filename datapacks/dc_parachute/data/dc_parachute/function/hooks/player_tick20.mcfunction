# Runs once per second on each player

# Allow this to activate on corner of blocks so players can check functionality
execute if block ~ ~-0.1 ~ #drakoncore:non_solid run function dc_parachute:check_for_parachute